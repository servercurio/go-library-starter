-- +goose Up
-- The starter template ships no schema. This is a no-op initial migration
-- so the embed.FS pattern in migration.go has at least one file to embed at
-- compile time. Add real migrations as new files named
-- YYYYMMDDHHMMSS_description.sql in this directory; goose will pick them up
-- automatically on the next run of Migrate().

-- +goose Down
