// Command appcli is the starter template's CLI entry point. It is a tiny
// shim that calls cli.NewRootCommand().Execute(); Cobra owns argv parsing,
// help, and shell-completion. The real work lives in internal/cli.
package main
